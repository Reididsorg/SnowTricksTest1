<header class="container-fluid d-none d-lg-block">
    <nav class="navbar navbar-expand navbar-custom">
        <a id="logo" href="/">
            <img class="" src="/img/logo-snowtricks.png">
        </a>
        <div class="collapse navbar-collapse">

            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                    <li class="nav-item active"><a class="nav-link" href="{{ path('app_homepage') }}">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_create_trick') }}">Créer un trick</a></li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown" id="accountDropdownLink" aria-haspopup="true" aria-expanded="false">
                            <i class="fas fa-user"></i> {{ app.user.username}}
                        </a>
                        <div class="btn dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                            <a class="dropdown-item" href="{{ path('app_edit_account') }}">Mon compte</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('app_forgot_password') }}">Mot de passe oublié</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('app_reset_password') }}">Réinitialiser mon mot de passe</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
                        </div>
                    </li>
                {% else %}
                    <li class="nav-item active"><a class="nav-link" href="{{ path('app_homepage') }}">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_registration') }}">Inscription</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Connexion</a></li>
                {% endif %}
            </ul>
        </div>
    </nav>
</header>
